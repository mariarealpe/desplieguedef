# Variables de entorno para Render
# Este archivo es solo de referencia - NO subirlo a GitHub

# DESARROLLO (local) - Ya configurado en application.properties
SPRING_PROFILES_ACTIVE=default
DATABASE_URL=jdbc:h2:mem:bookdb

# PRODUCCIÓN (Render) - Se configura automáticamente vía render.yaml
SPRING_PROFILES_ACTIVE=prod
DATABASE_URL=${DATABASE_URL}
# ^ Esta variable la inyecta Render automáticamente desde la base de datos PostgreSQL

# NOTAS:
# - En Render, DATABASE_URL se configura automáticamente vía el Blueprint (render.yaml)
# - NO necesitas configurar variables manualmente si usas el Blueprint
# - El formato de DATABASE_URL en Render es: postgresql://user:password@host:port/database

